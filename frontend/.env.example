# Development — Vite proxy handles /api → localhost:8000, so this is optional in dev
VITE_API_BASE_PATH=/api/v1

# Production — set this to your Render backend URL
# VITE_API_BASE_URL=https://finagent-server.onrender.com/api/v1
.